import 'package:flutter/material.dart';

class PendientePantalla extends StatelessWidget {
  const PendientePantalla({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}